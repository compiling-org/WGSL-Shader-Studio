use crate::screenshot_video_export::ScreenshotVideoExporter;

#[test]
fn test_screenshot_module_exists() {
    let exporter = ScreenshotVideoExporter::new();
    // This test just checks if the module compiles
}