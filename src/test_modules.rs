// Test file to verify module imports are working
use crate::wgpu_integration::WgpuRenderPipeline;
use crate::visual_node_editor::VisualNodeEditor;
use crate::shader_browser::{ShaderBrowser, ShaderCategory};
use crate::bevy_node_graph_integration::NodeGraphPlugin;

pub fn test_modules() {
    println!("All modules imported successfully!");
}